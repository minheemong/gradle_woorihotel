<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ezen.spg.dao.IMemberDao">
	<select id="getMember" resultType="com.ezen.spm1.dto.MemberVO">
		select * from member where id=#{param1}
	</select>
	
	<select id="selectAddressByDong" resultType="com.ezen.spm1.dto.AddressVO">
		select * from address where dong like '%'||#{param1}||'%'
	</select>
	
	<insert id="insertMember" parameterType="com.ezen.spm1.dto.MemberVO">
		insert into member (id, pwd, name, email, zip_num, address, phone) 
		values(#{id}, #{pwd}, #{name}, #{email}, #{zip_num}, #{address}, #{phone})
	</insert>
	
	<update id="updateMember" parameterType="com.ezen.spm1.dto.MemberVO">
		update member set pwd=#{pwd},name=#{name},zip_num=#{zip_num},
		address=#{address},email=#{email},phone=#{phone} where id=#{id}
	</update>
</mapper>